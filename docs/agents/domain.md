# Domain Docs

This is a single-context repository for `moritzbrantner/workflow-editor`.

## Before Exploring, Read These

- `CONTEXT.md` for project vocabulary and current context.
- `docs/adr/` for accepted architectural decisions.
- `CONTRIBUTING.md` for project contribution and verification rules.

Proceed silently when an optional file or directory is absent.

## Repository-Specific Vocabulary

Use this file for stable repo-level orientation and defer detailed domain terminology to the project README, context files, ADRs, and nearby tests. If the domain model becomes ambiguous during planning or implementation, capture the decision in an ADR or context document instead of burying it in an issue comment.
